[project]
name = "smallzh-python"
version = "0.1.0"
description = "smallzh's python notes"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mkdocs>=1.6.1",
    "mkdocs-smzhbook-theme>=1.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
